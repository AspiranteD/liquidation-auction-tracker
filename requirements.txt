requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
fpdf2>=2.7.0

# Verificación de precios contra la BD Reusalia (lookups por ASIN). Opcional:
# pricing.py degrada con gracia si no está, usando caché + scraping de Amazon.
psycopg2-binary>=2.9.0

# Solo para reconstruir data/recovery.json (scripts/build_recovery.py):
#   pandas>=2.0.0
#   numpy>=1.24.0
