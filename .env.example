# --- Monitoring ---
# Comma-separated B-Stock country codes to scrape (ES, IT, DE, FR, ...)
MONITOR_COUNTRIES=ES

# Where the SQLite DB and downloaded manifests live
DB_PATH=data/auctions.db
MANIFEST_DIR=data/manifests

# --- Bid calculator / alert rules ---
# Target landed cost as a fraction of retail, used to compute the suggested bid
BID_TARGET_TOTAL_PCT=0.25
# Only alert on auctions whose retail value is at least this (EUR)
ALERT_MIN_RETAIL=5000
# Only alert if the lot can still be landed at or below this fraction of retail
ALERT_MAX_TOTAL_PCT=0.30
# Minimum piece count to consider (0 = no minimum)
ALERT_MIN_PIECES=0

# --- Email alerts (optional) ---
EMAIL_ALERTS_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
# For Gmail use an App Password (https://myaccount.google.com/apppasswords)
SMTP_PASSWORD=your_app_password
EMAIL_SENDER=your_email@gmail.com
EMAIL_RECIPIENTS=alerts@example.com,me@example.com
